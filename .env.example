# Public runtime config. Copy to .env.local for local dev (git-ignored).

# Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX).
# Only loaded in production builds; leave unset locally.
NEXT_PUBLIC_GA_ID=
