# SEO growth run — 28 July 2026

Compared against the [27 Jul run + same-day addendum](2026-07-27-weekly.md).
First run under the new 3x/week (Mon/Wed/Fri) cadence.

## Numbers vs last run

| Metric                            | 27 Jul (morning)    | 27 Jul (addendum, evening) | 28 Jul                                    |
| ---------------------------------- | -------------------- | --------------------------- | -------------------------------------------- |
| Published posts                    | 6                    | 6                            | 6 (unchanged)                                 |
| GA4 users (28d)                    | 19                   | 19                           | 19 (same rolling window still overlapping)   |
| Australia share                    | 26.3%                | 26.3%                        | 26.3% (5/19 users, unchanged)                 |
| Sitemap submitted / indexed        | 13 / 3               | **13 / 0**                   | 13 / 0 (unchanged from addendum)              |
| Previously-indexed posts, re-check | n/a                  | "Unknown to Google" (2 of 2 checked) | "Unknown to Google" (2 of 2 re-checked) |
| GSC AU queries                     | 0 rows                | not re-checked               | 0 rows (unchanged)                            |
| Bing crawl                         | never crawled         | never crawled                | **first crawl record exists** (2026-07-27), but `CrawledPages: 0`, `InIndex: 0` |
| Technical audit                    | 5 warnings, 0 errors | not re-run                   | 5 warnings, 0 errors (identical findings)     |
| Open GitHub issues                 | 1 (#1)               | 1 (#1)                        | 1 (#1, unchanged, not duplicated)             |

## The constraint

**Unchanged from yesterday's addendum — this is a confirmation run, not a
new finding.** Re-inspected the same two posts flagged yesterday evening
(`why-chatgpt-never-recommends-your-business`, `ai-tax-return-what-its-good-for`);
both still read "URL is unknown to Google," same as last night. The sitemap
rollup is still 13 submitted / 0 indexed. Nothing has recovered, nothing has
gotten worse — the coverage-state reversal from yesterday is holding steady
rather than resolving on its own.

One small, genuinely new data point: Bing's crawl report now has a row for
2026-07-27 (previously the array was entirely empty), but every field on it
is zero — `CrawledPages: 0`, `InIndex: 0`, `InLinks: 0`. Read this as "Bing's
reporting pipeline has started producing daily rows," not "Bing has started
crawling." Still fully dark on actual crawl activity, now 3+ days past
verification.

The technical audit is identical to yesterday: 0 errors, 5 title-length
warnings, same 5 posts, same characters. No new regressions from the build.

## What I changed

**Nothing shipped this run.** There is no new finding to act on: the audit
is unchanged, the indexing reversal from yesterday has neither resolved
(so there's nothing to "fix" or confirm-and-close) nor worsened into
something with a new, actionable cause. Re-submitting the sitemap or
re-pushing IndexNow would not address a Google-side coverage-state
reversal, and issue #1 already covers the one concrete, actionable
technical finding (the two over-length titles). Filing a second issue
for "Google hasn't re-indexed us yet" would not be actionable for James —
there's no engineering fix for it — so it stays a watch item, not an issue.

No repo commits needed beyond this report.

## Content handoff (for `blog-research` / `blog-post`)

**Still nothing to mine.** `gsc queries --country AUS` returned zero rows
again. This won't change until pages are actually indexed and start
receiving impressions — see the traffic recommendations below for how to
work around this gap in the meantime rather than waiting on it.

## What I'll watch next run (Fri 31 Jul, or Mon 3 Aug if the run is skipped)

1. **Sitemap indexed count.** 0/13 for two consecutive checks now (evening
   27th, morning 28th). If still 0 by the next run — 4+ days after the
   posts were first confirmed indexed — that crosses from "plausible young-
   domain noise" into "worth raising directly with James," since there's no
   further diagnostic this skill can run against a Google-internal state.
2. **Bing actual crawl activity** (`CrawledPages`/`InIndex` > 0, not just a
   non-empty row). Now 3+ days post-verification with zero real activity.
3. **First blog-page impressions in GSC** — unchanged as the signal that
   would clear the indexing constraint.
4. **Issue #1** — still open, no action needed from this skill.

## Blocked on / needs a decision

Nothing new. Same as yesterday: issue #1 is filed and waiting on James's
time, not blocking. If the sitemap is still 0/13 at the next run, I'll
lead with that explicitly rather than let it become a fourth quiet entry
in a watch list.
