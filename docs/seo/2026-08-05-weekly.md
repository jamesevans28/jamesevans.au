# SEO growth run — 5 August 2026

Compared against the [3 Aug run](2026-08-03-weekly.md).

## Blocked on / needs a decision

**Two items still need James, carried forward unchanged:**

1. **Bing has never crawled the site — now 11 days post-verification.**
   `CrawledPages`/`InIndex` are zero across the full week again,
   `DiscoveryDate` and `LastCrawledDate` are both still `null` on the
   homepage. No self-service lever remains (IndexNow key is live and has
   been pushed). This needs James to contact Bing Webmaster Tools support
   directly — that requires the verified account holder, which this skill
   doesn't have standing authority to initiate. Flagged 3 Aug; unchanged.
2. **Publishing cadence — now four consecutive flat-content weeks
   (28 Jul, 29 Jul, 31 Jul, 3 Aug, and now 5 Aug with no new post since).**
   See "The constraint" below. This is the run where the skill's own
   calibration says to stop treating it as a routine watch-item.

## Numbers vs last run

| Metric                      | 3 Aug                                                        | 5 Aug                                                                                                                                                                                     |
| --------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Published posts             | 6 (3rd consecutive flat-content week)                        | 6 (unchanged — **4th** consecutive flat-content week)                                                                                                                                     |
| GA4 users (28d)             | 22                                                           | 23                                                                                                                                                                                        |
| Australia share             | 22.7% (5/22) — same 5 AU users, denominator grew             | 21.7% (5/23) — same 5 AU users again, denominator grew again                                                                                                                              |
| GSC total query rows        | 3 ("ato chatbot", "ai tax australia", "james patrick evans") | 3, unchanged (same three queries, same positions)                                                                                                                                         |
| GSC AU queries              | 2 rows (`ato chatbot` pos 10, `ai tax australia` pos 87)     | Same 2 rows, unchanged                                                                                                                                                                    |
| New GSC page surfaced       | —                                                            | `/about/` now shows 23 impressions/28d at position 19.7 (query breakdown returns only the 1-impression `james patrick evans` row — the rest is below GSC's per-query reporting threshold) |
| Per-URL index status        | Re-verified `ai-tax-return-what-its-good-for`: PASS, indexed | Re-verified `/about/`: PASS, "Submitted and indexed", last crawled 3 Aug                                                                                                                  |
| Sitemap submitted / indexed | 13 / 0 (unchanged; per-URL inspection remains ground truth)  | 13 / 0, unchanged                                                                                                                                                                         |
| Bing crawl                  | all zero, 8 days post-verification, `DiscoveryDate: null`    | all zero, **11 days** post-verification, `DiscoveryDate` and `LastCrawledDate` still both `null`                                                                                          |
| Technical audit             | 5 warnings, 0 errors — identical to 31 Jul                   | 5 warnings, 0 errors — identical again, no regression                                                                                                                                     |
| Open GitHub issues          | 1 (#1, unchanged)                                            | 1 (#1, unchanged, checked for duplicates — none filed)                                                                                                                                    |
| Deploy status               | green                                                        | green — includes a `blog-publish` repository_dispatch deploy on 3 Aug (rebuild only, confirmed no new post landed; sitemap still lists the same 6 posts)                                  |
| Tests / typecheck / lint    | 213/213 pass, typecheck clean, lint clean                    | tests pass (96.2% stmt coverage), typecheck clean, lint clean (`src`/`scripts` scope)                                                                                                     |

## The constraint hasn't moved — four flat content weeks now

Traffic-side numbers are static noise at this volume: GA4 users ticked
22 → 23, all from the same non-AU trickle (Netherlands, US, Poland, France
each contributing a handful); AU's absolute user count is still 5, so the
21.7% share is the same signal as last week's 22.7%, just a smaller
fraction of a slightly bigger denominator. Per "respect small numbers,"
this is not a trend.

GSC query rows are unchanged at 3 total for the third run running —
identical queries, identical positions. The one new observation is `/about/`
picking up 23 impressions (up from not separately visible before), but the
query-level breakdown only surfaces one row above GSC's reporting floor —
consistent with a handful of low-volume name-search variants, not a new
ranking signal to act on.

The technical side remains genuinely clean: audit is byte-for-byte the same
5 warnings (all covered by open issue #1, no new findings), a freshly
spot-checked page (`/about/`) re-verified as indexed and healthy, and the
full verification suite (tests, typecheck, lint) is green. There is nothing
technical left to fix that isn't already tracked in #1.

I also confirmed the `blog-publish` deploy that ran on 3 Aug was a rebuild
dispatch, not a new post — the sitemap still lists exactly the 6 posts from
last run. No sixth-run illusion of progress here.

**Bing crossed from "looks stuck" to "unambiguously stuck."** 11 days with
zero discovery is well past any reasonable propagation window; nothing
further changed on this side because there is nothing left to change from
this skill's authority. Escalation stands, unchanged in substance from 3 Aug.

## What I changed

- Ran `npm run build && npm run seo -- audit` — identical 5 warnings, 0
  errors, confirming no regression.
- Confirmed no 7th post shipped despite a `blog-publish` deploy event on
  3 Aug — checked the live sitemap directly; still 6 posts.
- Re-verified index status via `gsc inspect` on `/about/` (the page
  carrying this run's one new signal) — confirmed indexed and healthy.
- Checked `bing traffic`, `bing crawl`, `bing quota`, and `bing inspect`
  on the homepage — confirmed the flat-crawl pattern has extended to 11
  days with no new data points; escalation from 3 Aug stands.
- Checked `gh issue list` for duplicates before concluding no new issue was
  needed — none found; #1 still covers every audit finding.
- Ran the full verification suite (typecheck, lint scoped to
  `src`/`scripts`, full test suite) — all green.
- **No commits to site code or content this run.** Nothing was in scope:
  no new posts to link or push via IndexNow, no audit regressions, no new
  title/meta findings beyond issue #1, and query volume (3 rows, unchanged
  for three runs straight) is still too thin to justify a striking-distance
  rewrite with any confidence.

This is the fourth week in a row where the technical side is clean and the
traffic side is flat. The skill's guidance is explicit that three flat
content weeks is the point to say the constraint plainly rather than as a
routine watch-item — this is now the fourth, so restating it here for the
second time, more plainly: **the site is not being held back by anything
this skill can fix. It is held back by having 6 posts.**

## Content handoff (for `blog-research` / `blog-post`)

Query data is unchanged from the last two runs and still too thin (3 total
rows site-wide) to extract new H2 phrasings or content gaps with
confidence. Carrying forward, unchanged:

- `ato chatbot` (pos 10, stable across three runs) remains the strongest
  signal the site has. `ai-tax-return-what-its-good-for` already ranks
  for it; if impressions keep accumulating on this exact query,
  strengthening that page with an H2 that explicitly answers "can I use a
  chatbot for my tax return" is lower-risk than commissioning a new post.
- `ai tax australia` (pos 87, unchanged) is still effectively unranked —
  not evidence of anything yet, just a query to keep watching.
- `james patrick evans` (pos 34, new this run) is a name-search query
  landing on `/about/` — expected for a personal site, not a content gap.
- **The binding constraint, stated for the second consecutive run:
  publishing cadence, not targeting, not technical health.** Four
  consecutive runs (28 Jul, 31 Jul, 3 Aug, 5 Aug) with no new post. 6 live
  posts is not enough surface area to generate meaningful impression
  growth regardless of how well the existing 6 are optimised — and they
  are already clean. The single highest-leverage action available right
  now is a 7th post. Any well-scoped AI/AU topic clears the bar; the query
  data isn't rich enough yet to out-rank `blog-research`'s own topic
  sourcing.

## What I'll watch next run

1. **Whether a 7th post ships.** Four consecutive flat-content weeks as of
   this run. If still flat next run, this becomes the dominant finding of
   the report rather than a section within it.
2. **Bing crawl activity**, pending James's outreach to Bing support. If a
   support contact resolves it, watch for `CrawledPages` and
   `DiscoveryDate` to move off zero/null.
3. **`/about/` impressions** — 23 this run, first time visible at this
   volume. Watch whether it holds or was a one-off before drawing any
   conclusion.
4. **Query row count** — flat at 3 for three runs straight. If it moves to
   5+, revisit for a genuine striking-distance candidate.
5. **Issue #1** — still open, unchanged, waiting on James's sign-off on
   the two over-length titles per the issue's own scope note.
6. **AU share** — 22.7% → 21.7% this run on unchanged absolute AU users
   (5, three runs straight). Noise at this volume; will only act on it if
   the pattern persists over several more runs with a growing AU count.
