# SEO growth run — 3 August 2026

Compared against the [31 Jul run](2026-07-31-weekly.md).

## Blocked on / needs a decision

**Bing has never crawled the site — worth a support inquiry now.** It's
been 8 days since verification (well past the ~48h Bing quotes for a new
property), `CrawledPages`/`InIndex` are still zero across the whole week,
and this run's `bing inspect` on the homepage shows `DiscoveryDate: null`
in addition to `LastCrawledDate: null` — Bing hasn't even discovered the
homepage, let alone crawled it. The IndexNow key is live and has already
been pushed; there is no further self-service lever left on this side.
This was flagged as a watch-item last run ("one more flat cycle... worth a
support inquiry") and that threshold has now been crossed. This needs
James directly — Bing Webmaster Tools support contact typically requires
the verified account holder, which this skill doesn't have standing
authority to initiate.

## Numbers vs last run

| Metric                       | 31 Jul                                       | 3 Aug                                                                             |
| ----------------------------- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| Published posts              | 6                                             | 6 (unchanged — 3rd consecutive flat-content week)                                |
| GA4 users (28d)              | 19                                            | 22                                                                                |
| Australia share               | 26.3% (5/19)                                  | 22.7% (5/22) — same 5 AU users, denominator grew                                  |
| Per-URL index status          | 6 of 6 "Submitted and indexed"                | Re-verified `ai-tax-return-what-its-good-for`: still PASS, indexed, last crawled 26 Jul |
| Sitemap submitted / indexed   | 13 / 0                                        | 13 / 0 (unchanged; per-URL inspection remains ground truth, not this rollup)      |
| GSC impressions/clicks (28d)  | 6 pages with impressions, 0 clicks            | 7 pages with impressions, 0 clicks                                                |
| GSC AU queries                | 2 rows ("ato chatbot" pos 10, "ai tax australia" pos 87) | Same 2 rows, unchanged (`ato chatbot` pos 10, `ai tax australia` pos 87)          |
| GSC total query rows          | 3 ("ato chatbot", "ai tax australia", "james patrick evans") | 3, unchanged                                                                      |
| Bing crawl                    | all zero, 6 days post-verification, homepage `LastCrawledDate: null` | all zero, **8 days** post-verification; homepage now also shows `DiscoveryDate: null` |
| Technical audit               | 5 warnings, 0 errors                          | 5 warnings, 0 errors — identical findings, no regression                         |
| Open GitHub issues            | 1 (#1)                                        | 1 (#1, unchanged, checked for duplicates — none filed)                           |
| Deploy status                 | green                                         | green (`gh run list` confirms CI + Deploy success on the last push)              |
| Tests / typecheck / lint      | 213/213 pass, typecheck clean, lint clean     | 213/213 pass, typecheck clean, lint clean (`src`/`scripts` scope)                |

## The constraint hasn't moved — three flat content weeks now

Nothing materially changed on the traffic side since 31 Jul. GA4 users
ticked up 19 → 22, but the extra users are non-AU (Netherlands, US, Poland
each contributed a handful) — AU share actually *dropped* in percentage
terms (26.3% → 22.7%) even though the absolute AU user count (5) didn't
change. At this volume that's noise, not a trend; flagging per the skill's
"respect small numbers" guidance rather than treating it as a real shift.

GSC query rows are unchanged at 3 total, 2 AU-filtered — the same
`ato chatbot` (pos 10) and `ai tax australia` (pos 87) rows as last run,
with identical positions. No new query data to act on.

The technical side remains genuinely clean: audit is byte-for-byte the
same 5 warnings (all covered by open issue #1, no new findings), the one
previously-checked indexed page re-verified as PASS, and the full
verification suite (213 tests, typecheck, lint) is green. There is nothing
technical left to fix that isn't already tracked.

**What did change, and matters:** Bing crossed from "still early" to
"looks stuck" — see the escalation above. That's this run's one real
finding.

## What I changed

- Re-ran `npm run build && npm run seo -- audit` — identical 5 warnings, 0
  errors. No regressions, nothing new to fix.
- Re-verified index status via `gsc inspect` on the page carrying this
  site's strongest query signal (`ai-tax-return-what-its-good-for`, for
  `ato chatbot`) — confirmed still indexed and healthy.
- Checked `bing quota`, `bing traffic`, `bing crawl`, and `bing inspect`
  on the homepage — confirmed the flat-crawl pattern has extended to 8
  days with a new data point (`DiscoveryDate: null`) that raises this
  from a watch-item to something worth escalating to James.
- Checked for duplicate GitHub issues before concluding no new one was
  needed — none found; existing issue #1 already covers every audit
  finding.
- Ran the full verification suite (typecheck, lint scoped to
  `src`/`scripts`, 213 tests) — all green.
- **No commits to site code or content this run.** Nothing was in scope:
  no new posts to link or push via IndexNow, no audit regressions, no new
  title/meta findings beyond issue #1, and query volume (3 rows,
  unchanged for two runs straight) is still too thin to justify a
  striking-distance rewrite with any confidence.

This is a second "site is technically healthy" week in a row. Per the
skill's own calibration: a flat week alone isn't a failure, but this is
now the **third consecutive week with no new content published**, and the
skill explicitly says three flat content weeks is the point to say so more
forcefully than a watch-item — see below.

## Content handoff (for `blog-research` / `blog-post`)

Query data is unchanged from last run and still too thin (3 total rows
site-wide) to extract new H2 phrasings or content gaps with confidence.
Carrying forward, unchanged:

- `ato chatbot` (pos 10, stable across two runs) remains the strongest
  signal the site has. `ai-tax-return-what-its-good-for` already ranks
  for it; if impressions keep accumulating on this exact query,
  strengthening that page with an H2 that explicitly answers "can I use a
  chatbot for my tax return" is lower-risk than commissioning a new post.
- `ai tax australia` (pos 87, unchanged) is still effectively unranked —
  not evidence of anything yet, just a query to keep watching.
- **The binding constraint, stated plainly: publishing cadence, not
  targeting, not technical health.** Three consecutive runs (28 Jul,
  31 Jul, 3 Aug) with no new post. 6 live posts is not enough surface
  area to generate meaningful impression growth regardless of how well
  the existing 6 are optimised — and they are already clean. The single
  highest-leverage action available right now is a 7th post. Any
  well-scoped AI/AU topic clears the bar; the query data isn't rich
  enough yet to out-rank `blog-research`'s own topic sourcing.

## What I'll watch next run

1. **Whether a 7th post ships.** Three consecutive flat-content weeks as
   of this run — per the skill's guidance this is the point to flag it
   forcefully rather than as a routine watch-item, which is what the
   "Blocked on" framing above and this note are doing.
2. **Bing crawl activity**, pending James's outreach to Bing support.
   If a support contact resolves it, watch for `CrawledPages` and
   `DiscoveryDate` to move off zero/null.
3. **Query row count** — flat at 3 for two runs straight. If it moves to
   5+, revisit for a genuine striking-distance candidate.
4. **Issue #1** — still open, unchanged, waiting on James's sign-off on
   the two over-length titles per the issue's own scope note.
5. **AU share** — 26.3% → 22.7% this run on unchanged absolute AU users.
   Noise at this volume; will only act on it if the pattern persists
   over several more runs.
