# SEO growth run — 7 August 2026

Compared against the [5 Aug run](2026-08-05-weekly.md).

## Blocked on / needs a decision

**Two items still need James, carried forward unchanged:**

1. **Bing has never crawled the site — now 14 days post-verification.**
   `CrawledPages`/`InIndex` are zero across the full trailing week again,
   and `bing traffic` shows zero impressions/clicks every day back to
   25 Jul. No self-service lever remains (IndexNow key is live and has
   been pushed repeatedly). This needs James to contact Bing Webmaster
   Tools support directly — that requires the verified account holder,
   which this skill doesn't have standing authority to initiate. Flagged
   3 Aug, 5 Aug; unchanged again.
2. **Publishing cadence — now five consecutive flat-content weeks**
   (28 Jul, 29 Jul, 31 Jul, 3 Aug, 5 Aug, and now 7 Aug with no new post
   since 25 Jul). See "The constraint" below.

## Numbers vs last run

| Metric                      | 5 Aug                                                        | 7 Aug                                                                                       |
| ---------------------------- | ------------------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| Published posts             | 6 (4th consecutive flat-content week)                        | 6 (unchanged — **5th** consecutive flat-content week)                                       |
| GA4 users (28d)             | 23                                                            | 23 (unchanged)                                                                              |
| Australia share             | 21.7% (5/23)                                                 | 21.7% (5/23) — same 5 AU users, same denominator this time                                  |
| GSC total query rows        | 3 ("ato chatbot", "ai tax australia", "james patrick evans") | **5** — adds "james evans" (pos 7, 4 impressions) and "jamie evans" (pos 48, 1 impression) |
| GSC AU queries              | 2 rows                                                       | 4 rows (adds the two name-search variants above)                                            |
| Per-URL index status        | Re-verified `/about/`: PASS, indexed, last crawled 3 Aug     | Re-verified `/` and `/about/`: both PASS, indexed; `/about/` freshly crawled 5 Aug            |
| Sitemap submitted / indexed | 13 / 0 (per-URL inspection remains ground truth)             | 13 / 0, unchanged                                                                           |
| Bing crawl                  | all zero, 11 days post-verification, both dates `null`      | all zero, **14 days** post-verification, `bing traffic` flat zero every day back to 25 Jul  |
| Technical audit             | 5 warnings, 0 errors — identical to 31 Jul                   | 5 warnings, 0 errors — identical again, no regression                                       |
| Open GitHub issues          | 1 (#1, unchanged)                                            | 1 (#1, unchanged, checked for duplicates — none filed)                                      |
| Deploy status               | green                                                        | green — no new deploy since 4 Aug (nothing shipped this week)                               |
| Tests / typecheck / lint    | 213/213 pass, typecheck clean, lint clean                    | 213/213 pass (96.2% stmt coverage), typecheck clean, lint clean (scoped to `src`/`scripts`)  |

## The constraint hasn't moved — five flat content weeks now

Traffic is flat by every measure available: GA4 users held exactly at 23,
AU share held exactly at 21.7% on the same 5 AU users. Per "respect small
numbers," this is not decline, it's just no signal at this volume.

The one genuine change this run: GSC picked up two new name-search query
rows (`james evans` at position 7 with 4 impressions, `jamie evans` at
position 48 with 1 impression). Both are personal-name searches landing
presumably on `/` or `/about/`, not evidence of topical ranking progress —
expected background noise for a site under someone's own name, not a
content gap or a CTR opportunity (position 7 with 0 clicks on a 4-impression
name query isn't a meaningful CTR signal either way).

The technical side remains genuinely clean: audit is byte-for-byte the same
5 warnings (all covered by open issue #1, no new findings), both `/` and
`/about/` re-verified as indexed and healthy on spot-check, and the full
verification suite (tests, typecheck, lint) is green. There is nothing
technical left to fix that isn't already tracked in #1.

**Bing remains completely dark at 14 days post-verification.** `bing
traffic` shows literal zero impressions and zero clicks every single day
in the trailing window back to 25 Jul — not just zero crawl stats, zero
search activity of any kind. This is now unambiguously a case for Bing
support, not a propagation-timing issue; nothing further changed on this
side because there is nothing left to change from this skill's authority.

## What I changed

- Ran `npm run build && npm run seo -- audit` — identical 5 warnings, 0
  errors, confirming no regression.
- Confirmed no 7th post shipped — checked `gh run list` (no `blog-publish`
  dispatch since 3 Aug) and the live sitemap directly (still 6 posts,
  same 13 submitted URLs).
- Re-verified index status via `gsc inspect` on both `/` and `/about/` —
  both PASS, indexed, robots allowed, no canonical mismatch.
- Checked `bing traffic`, `bing crawl`, and `bing quota` — confirmed the
  flat-crawl pattern has extended to 14 days with zero search activity of
  any kind, not just zero crawl; escalation from prior runs stands.
- Checked `gh issue list` for duplicates before concluding no new issue
  was needed — none found; #1 still covers every audit finding and is
  still accurate (verified against current audit output).
- Ran the full verification suite: typecheck clean, tests 213/213 pass
  (96.2% stmt coverage), lint clean when scoped to `src`/`scripts` per
  the skill's own scoping note (an unscoped `eslint .` run picks up a
  stray `.claude/worktrees/` checkout and build output outside the
  project's own ignore list — a pre-existing gap in `eslint.config.*`,
  not touched this run since it's not blocking and is arguably a
  functional tooling change).
- **No commits to site code or content this run.** Nothing was in scope:
  no new posts to link or push via IndexNow, no audit regressions, no new
  title/meta findings beyond issue #1, and the two new query rows are
  name-search noise, not a striking-distance rewrite candidate.

This is the fifth week running where the technical side is clean and the
traffic side is flat. Restating plainly, for the third time: **the site is
not being held back by anything this skill can fix. It is held back by
having 6 posts.** Continuing to report this as a routine line item each
week would understate it — five weeks of a stalled top-of-funnel is the
actual finding of this run, not a footnote to a clean technical audit.

## Content handoff (for `blog-research` / `blog-post`)

Query data is effectively unchanged in substance — the two new rows this
run are personal-name searches, not topical signal. Carrying forward,
unchanged:

- `ato chatbot` (pos 10, stable across four runs) remains the strongest
  topical signal the site has. `ai-tax-return-what-its-good-for` already
  ranks for it; if impressions keep accumulating on this exact query,
  strengthening that page with an H2 that explicitly answers "can I use a
  chatbot for my tax return" is lower-risk than commissioning a new post.
- `ai tax australia` (pos 87, unchanged) is still effectively unranked —
  not evidence of anything yet, just a query to keep watching.
- `james evans` / `james patrick evans` / `jamie evans` — three name-search
  variants now visible, all landing near `/about/` or `/`. Expected for a
  personal site under one's own name, not a content gap.
- **The binding constraint, stated for the third consecutive run:
  publishing cadence, not targeting, not technical health.** Five
  consecutive runs (28 Jul, 31 Jul, 3 Aug, 5 Aug, 7 Aug) with no new post
  since 25 Jul. 6 live posts is not enough surface area to generate
  meaningful impression growth regardless of how well the existing 6 are
  optimised — and they are already clean. The single highest-leverage
  action available right now is a 7th post. Any well-scoped AI/AU topic
  clears the bar; the query data isn't rich enough yet to out-rank
  `blog-research`'s own topic sourcing.

## What I'll watch next run

1. **Whether a 7th post ships.** Five consecutive flat-content weeks as of
   this run — the dominant finding, not a section within it.
2. **Bing crawl activity**, pending James's outreach to Bing support. If a
   support contact resolves it, watch for `CrawledPages` and
   `bing traffic` impressions to move off zero.
3. **The two new name-search query rows** — watch whether `james evans`
   holds at position 7 or moves; low priority, just a data point.
4. **Query row count** — moved 3 → 5 this run, but on name-search noise,
   not topical queries. Watch for genuine topical rows (5+ distinct
   non-name queries) before treating this as a striking-distance signal.
5. **Issue #1** — still open, unchanged, waiting on James's sign-off on
   the two over-length titles per the issue's own scope note.
6. **AU share** — flat at 21.7% on 5 AU users for two runs straight now.
   Will only act on it if the pattern persists over several more runs
   with a growing AU count.
